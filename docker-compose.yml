services:
  monitor:
    build: .
    ports:
      - "8200:8200"
    env_file:
      - .env
    volumes:
      - contract-data:/app/data
    environment:
      - STATE_FILE=/app/data/seen_contracts.json
      - SCAN_INTERVAL_MINUTES=30
    restart: unless-stopped

volumes:
  contract-data:
